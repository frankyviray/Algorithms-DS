// Find 1st, Find Nth...

// const array = ['hi', 'my', 'teddy'];
// array[0]; // O(1)
// array[array.length - 1] // O(1)

const array = [{
    tweet: 'hi',
    date: 2012
}, {
    tweet: 'my',
    date: 2014
}, {
    tweet: 'teddy',
    date: 2018
}];
// O(n^2);

'helasdfeavxzcvzvzqweq'.length
// big O depends on language (method could be different based on language for '.length')
// in JS, .length runs as a built-in property (runs as O(1))